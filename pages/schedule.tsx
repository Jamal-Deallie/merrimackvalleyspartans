import Calendar from '@/containers/CalendarContainer';

type Props = {};

const Schedule = (props: Props) => {
  return (
    <section>
      <h1>Schedule</h1>
      <Calendar />
    </section>
  );
};

export default Schedule;
